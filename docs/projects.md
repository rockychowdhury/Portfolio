Schema:
{
  id:
  title:
  description: (small - not rendered in ui just for reference)
  readmeLink: for render details page
  thumbnail: for placeholder of the container card while video does not start playing or not in focus.
  githubLink: CTA
  liveLink:CTA
  videopreviewlink: auto play the video on the UI pause on hover.
  youtubelink: CTA
  order: for render order maintain.
}






Inspired Site: https://www.apple.com/
Section: Endless entertainment.

In my personal portfolio the previous Projects section is not best representation of my projects. So I wanna rebuild this section with New UI idea.


In the main page's Projects section after the headline big container components line will visible like the reference web section (big card slider with images). But instead of showing like a card with images and texts I will keep it clean with a video preview (no overlay clean video) and on hover only two simple buttons named "Explore" and "Open" will show with clean animation - on click on the "Explore" button open a new tab with this project design [see the project details section for know about it]. on the open button will redirect to live page.

Initially only the middle project's card will be visible while scrolling. when the project card place to center then the left and right side projects will be visible and follow slider behavior. when user come to this section then only the center card will show comes from bottom of the screen and when place to almost center the left and right project will start visible with animation.
The video will only play of the center's project. 
Like the current slider auto slide after a duration, the video will slides auto if the current project preview is end, with the same animation of the ref slider the preview will shift to next project also on mouse click to left/right side visible projects container the project will be come on focus and slide the projects in circular list (after end the 1st will display)

Right after the project preview cards a small cards line with same sliding features will be rendered where each card will contain a problem or feature I solved or implemented in a specific project. It will fill with a image and text and CTA.
the design of those card will be smaller than the project preview card but card layout must be premium and the message should be delivered clearly using those card showing my works breaking down into problems and features.















